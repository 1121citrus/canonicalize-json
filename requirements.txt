# Installation requirements.
#
# Hash pinning via `pip install --require-hashes` (enforced in Dockerfile) guards
# against supply-chain substitution: a compromised PyPI mirror or a future
# version-collision attack cannot silently serve different content when the
# exact SHA-256 digests are required.
#
# To regenerate hashes after a version bump:
#   pip download --no-deps jcs==<new-version>
#   pip hash jcs-<new-version>*.whl jcs-<new-version>.tar.gz

jcs==0.2.1 \
    --hash=sha256:e23a3e1de60f832d33cd811bb9c3b3be79219cdf95f63b88f0972732c3fa8476 \
    --hash=sha256:9f20360b2f3b0a410d65493b448f96306d80e37fb46283f3f4aa5db2c7c1472b
